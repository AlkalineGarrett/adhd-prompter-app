package org.alkaline.taskbrain.ui.currentnote.undo

/**
 * Command types for grouping command bar actions.
 */
enum class CommandType {
    BULLET,
    CHECKBOX,
    INDENT,
    UNINDENT,
    MOVE,
    OTHER
}
